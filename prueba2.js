const myVariable = 10; // Utiliza const en lugar de let
const myFunction = () => {
  console.log('Función de flecha'); // Cumple con el uso de funciones de flecha
};

if (myVariable === 10) { // Cumple con la indentación de 2 espacios
  myFunction(); // Cumple con el uso de funciones y el punto y coma
}
